<%= form_with model: @task do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>内容</h4>
  <%= f.text_area :description %>
  <h4>進行状況</h4>
  <%= f.check_box :done %>
  <h4>期日</h4>
  <%= f.date_field :due_date %>
  <h4>メモ</h4>
  <%= f.text_area :memo %>
  <%= f.submit '投稿' %>
<% end %>